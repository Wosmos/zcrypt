{
  "name": "zcrypt-desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:sidecar": "bash build-sidecar.sh",
    "build:frontend": "bash build-frontend.sh",
    "prebuild": "npm run build:sidecar && npm run build:frontend",
    "dev": "npm run build:sidecar && cd src-tauri && cargo tauri dev",
    "build": "cd src-tauri && cargo tauri build",
    "tauri": "cd src-tauri && cargo tauri"
  }
}

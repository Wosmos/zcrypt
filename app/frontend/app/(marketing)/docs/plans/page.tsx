// zcrypt is free and open source — there are no plans, so this page redirects to the docs home.
// Safe to delete this file from the repo entirely.
import { redirect } from "next/navigation";

export default function Page() {
  redirect("/docs");
}

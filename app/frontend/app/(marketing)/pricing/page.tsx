// zcrypt is free and open source — there are no plans, so this page can be deleted from the repo entirely.
import { redirect } from "next/navigation";

export default function Page() {
  redirect("/");
}

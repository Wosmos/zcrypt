module.exports = {
  plugins: {
    // Tailwind CSS v4 (Rust "Oxide" engine). Embeds Lightning CSS, which does
    // vendor prefixing + modern-syntax lowering — so autoprefixer is gone.
    "@tailwindcss/postcss": {},
  },
};
